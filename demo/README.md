此文件夹包含 Caddy 网站的按需 TLS 演示的 Caddy 配置。

此配置托管在独立的机器上，以便在需要时可以在生产环境中隔离金丝雀构建。

此配置需要 [caddy-psl](https://github.com/mholt/caddy-psl) 和 [replace-response](https://github.com/caddyserver/replace-response) 插件。
